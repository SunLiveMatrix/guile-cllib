module allysquids.core;

/// The default implementation of the class that will be used when creating
class AllySquids {
    public static class instance(instance) { // @suppress(dscanner.style.phobos_naming_convention)
        enum AllySquids = "INSTANCE";
    }
}