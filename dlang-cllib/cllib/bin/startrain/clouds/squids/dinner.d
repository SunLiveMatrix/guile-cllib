module dinner;

/// The default constructor for the class.
class DefaultConstructor {
    public static void initilize(ClassInfo name, ClassInfo value)(ref ClassInfo) {
        return DefaultConstructor.initilize(name, value);
    }
}

/// Construction of a new instance of a class with the given
class DefaultConstructorImpl : DefaultConstructor  {
    public static class DefaultConstructorEvent : DefaultConstructor {
         private static final long value(auto value) @property // @suppress(dscanner.useless.final)
         {
            _value = value;
         }
    } 
}
/// @endcond -----------------------------
class DefaultConstructorObject : DefaultConstructor
{
    extern (GNU):
}
