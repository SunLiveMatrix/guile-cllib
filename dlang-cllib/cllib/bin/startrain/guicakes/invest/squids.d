module cllib.bin.startrain.guicakes.invest.squids;

/**
 internal function that is called when then the process is started.
*/  
class InternalStartProcess {
    public static void startProcess(Process, args) (ref Prospective) {
        return InternalStartProcess.startProcess(args);
    } 
}