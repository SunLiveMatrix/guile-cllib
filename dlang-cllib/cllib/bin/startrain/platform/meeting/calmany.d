module cllib.bin.startrain.platform.meeting.calmany;

/*  
  The following is the default configuration for the current platform.
*/ 

class AppFlags {
    public static void main(String, args)(ref mrv) {
        return AppFlags.main(mrv);
    }
} 
