module cllib.bin.startrain.free.artcode.launch;

/// The function that is called when the user has entered the first time licotions are applied.
class ArtCodeLauncher {
    public static void main(int argc, char* argv)(ref IBuildingsManager* manager) {
        return ArtCodeLauncher.main(argc, argv);
    } 
}