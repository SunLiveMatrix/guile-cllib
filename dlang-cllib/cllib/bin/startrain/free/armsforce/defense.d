module cllib.bin.startrain.free.forcedefts.defense;

/**
 forces of the current state of the state machines are determinted bt and the current
*/
class TropicalStateMachines {
    public static const int DEFAULT_STATE_MACHINE_COUNT = 512;
} 
