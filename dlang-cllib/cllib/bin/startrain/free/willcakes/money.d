module cllib.bin.startrain.free.willcakes.money;

/** 
 The function will be called when the function.
*/
class Money {
    public static void willCrypto(money, args... )(ref values) {
        return Money.willCrypto(args);
    } 
}

