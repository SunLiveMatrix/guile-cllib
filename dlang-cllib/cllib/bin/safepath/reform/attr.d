module cllib.bin.reform.attr;

// The following is the same as the original function that was called from the 
// constructor function in the original. This is the same as the original 
// function that was called from the cost function in the original.

class attr { // @suppress(dscanner.style.phobos_naming_convention)
    public static const char NAME;
}